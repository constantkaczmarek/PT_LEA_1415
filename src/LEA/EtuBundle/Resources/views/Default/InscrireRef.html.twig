{% extends "::base.html.twig" %}

{% block body %}
    <div class="container-fluid">
        <h1>{{ entr }}</h1>
        <form action="{{ path("lea_etu_inscrireRef",{'entr':entr}) }}" method="post">
            {{ form_widget(form) }}
            <input type="submit" name="Inscrire" value="Inscrire"/>
            <a href="{{ path("lea_etu_infosStage") }}">
                <button>Annuler</button>
            </a>
        </form>
        <div>
            C'est ca : {{ name }} et
        </div>
    </div>

{% endblock %}